import type { Metadata } from "next";

import { InfoPage } from "@/components/shop/InfoPage";
import { getContent } from "@/server/repositories/settings";
import { pageCrumbs } from "@/server/seo/breadcrumbs";

export const revalidate = 3600;

export const metadata: Metadata = {
  title: "Обмен и возврат",
  description:
    "Обмен и возврат обуви UGG® в течение 14 дней: условия, порядок действий и сроки возврата денег.",
  alternates: { canonical: "/obmen-i-vozvrat" },
};

export default function ReturnsPage() {
  const contacts = getContent().contacts;

  return (
    <InfoPage
      crumbs={pageCrumbs("Обмен и возврат")}
      title="Обмен и возврат"
      lead="Не подошёл размер или передумали — у вас есть 14 дней с момента получения."
    >
      <h2>Условия</h2>
      <p>
        По закону «О защите прав потребителей» обувь надлежащего качества можно
        обменять или вернуть в течение 14 дней, не считая дня покупки, если она не
        была в носке: сохранены товарный вид, коробка, бирки и ярлыки. Примерить
        дома на ковре — можно; выйти на улицу — уже нет.
      </p>

      <h2>Как обменять или вернуть</h2>
      <ol>
        <li>
          Напишите нам{contacts.email && <> на <a href={`mailto:${contacts.email}`}>{contacts.email}</a></>}
          {contacts.phone && <> или позвоните по телефону {contacts.phone}</>}: укажите номер заказа и
          что хотите — обмен на другой размер или возврат.
        </li>
        <li>Мы подскажем, как отправить обувь обратно через СДЭК, и пришлём этикетку.</li>
        <li>
          Обмен отправляем сразу после получения возврата. Деньги возвращаем тем же
          способом, которым вы платили, в течение 10 дней с момента, как посылка
          пришла к нам.
        </li>
      </ol>

      <h2>Если товар с браком</h2>
      <p>
        Мы продаём только оригинальную обувь и проверяем каждую пару перед
        отправкой. Если брак всё же обнаружился — сфотографируйте его и напишите
        нам: обмен или возврат в этом случае делаем за наш счёт, в том числе
        доставку.
      </p>

      <h2>Что вернуть нельзя</h2>
      <p>
        Обувь со следами носки, без коробки или с срезанными бирками обмену и
        возврату не подлежит — это требование закона, а не наше правило.
      </p>
    </InfoPage>
  );
}
