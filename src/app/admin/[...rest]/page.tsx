import { redirect } from "next/navigation";

/**
 * Любой несуществующий адрес внутри /admin — на главную.
 *
 * Раньше здесь был вход в панель, и старые закладки ведут именно сюда.
 * Отдавать по ним страницу входа или «не найдено» незачем: чем меньше
 * ответ отличается от обычного, тем меньше поводов искать дальше.
 */
export default function AdminUnknownPage() {
  redirect("/");
}
